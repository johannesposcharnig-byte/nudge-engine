name: Validate Nudge Engine

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run unit tests
        run: python3 -m unittest discover -s tests -p "test_*.py"

      - name: Compile source and tests
        run: PYTHONPYCACHEPREFIX=/tmp/nudge_engine_pycache python3 -m compileall src tests
